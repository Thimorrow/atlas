---
milestone: M001
slice: S02
task: T04
project: Atlas
created: 2026-06-26T17:42:00Z
status: planned
---

# M001-S02-T04 -- Task Plan

**Summary:** Integrationstest fuer S02: Routine + flexible-goal + manuelles Event +
Untis-Stunden (inkl. Entfall) anlegen; Wochen-Expansion + Freie-Luecken korrekt.

## Files

- Create: `lib/calendar.test.ts` -- (1) pure Unit-Tests fuer `freeSlotsForDay`
  (Busy-Merge, cancelled = frei, min-Filter, offenes Ende -> bis dayEnd); (2)
  Integrationstest gegen Neon: feste Routine + flexible_goal + manuelles Event +
  2 school_blocks (1 regular, 1 cancelled) auf einem Sentinel-Datum anlegen,
  `attachFreeSlots(await expandWeek(D))`, pruefen.

## What this does

Integration: Sentinel-Datum D (2099) -> Routine weekday=Wochentag(D), school+manual auf
D. Nach Expansion enthaelt Tag D: Routine (refId), manuelles Event (refId), 2 Schul-
Events (eines cancelled); FreeSlots decken die cancelled-Stunde ab, NICHT die regular;
flexibleGoals enthaelt das Ziel. beforeAll/afterAll raeumen auf (school_blocks per date,
routines/manual_events per id).

## Verification

```bash
npm test
npm run build
```

Success: alle Tests gruen (adapter + sync + calendar); Build gruen.

## Done signal

Danach `M001-S02-T04-SUMMARY.md` + Plan/STATE + Roadmap (S02 -> [x]) fortschreiben.
Slice S02 fertig -> S03 (Frontend).
